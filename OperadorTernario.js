

// Operador ternario: generar un if con un else en una sola linea
// Sintaxis: condition ? true : false;

var numero = 1;
var resultado = numero === 1 ? "Soy un numero uno" : "No soy un numero uno"
console.log(resultado);
